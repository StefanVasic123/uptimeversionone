import axios from 'axios';




/*
const lsData = Object.entries(localStorage).forEach((element) => element);

export const addItem = () => {
    axios
        .post('/api/items', lsData)
        .then(res => {
            name: lsData
        })
}
*/